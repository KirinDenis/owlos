<UserControl x:Class="OWLOSAdmin.EcosystemBrowser.BrowserControls.NodeConnectionConfigItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OWLOSAdmin.EcosystemBrowser.BrowserControls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="130*"/>
            <ColumnDefinition Width="607*"/>
        </Grid.ColumnDefinitions>

        
        <CheckBox x:Name="EnabledCheckBox" Grid.Column="0" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBox x:Name="NameTextBox" Grid.Column="1" Margin="5"/>
        <ComboBox x:Name="TypeComboBox" Grid.Column="2" Margin="5" SelectedItem="0" IsEnabled="False">
            <ComboBoxItem>HTTP (RESTful)</ComboBoxItem>
            <ComboBoxItem>MQTT</ComboBoxItem>
            <ComboBoxItem>UART</ComboBoxItem>
        </ComboBox>
        <TextBox x:Name="ConnectionStringTextBox" Grid.Column="3" Margin="5" />
        

    </Grid>
</UserControl>
